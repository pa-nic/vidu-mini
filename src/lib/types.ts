export interface tViewsByDate {
    day: string,
    views: number
}

export interface tViewsByCountry {
    country: string,
    views: number
}

export interface tViewsByPage {
    page: string,
    views: number
}